import{Navigate,Route,Routes}from'react-router-dom';import{useAuth}from'./auth';import LoginPage from'./pages/LoginPage';import PatientPortal from'./pages/PatientPortal';import DoctorPortal from'./pages/DoctorPortal';import AdminPortal from'./pages/AdminPortal';import Layout from'./components/Layout';
export default function App(){const{auth}=useAuth();if(!auth)return <Routes><Route path="*" element={<LoginPage/>}/></Routes>;const portal=auth.role==='PATIENT'?<PatientPortal/>:auth.role==='DOCTOR'?<DoctorPortal/>:<AdminPortal/>;return <Routes><Route path="/" element={<Layout/>}><Route index element={portal}/><Route path="calendar-connected" element={<div className="panel"><h2>Calendar connected</h2><p>Your future appointment events can now sync automatically.</p></div>}/></Route><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}
